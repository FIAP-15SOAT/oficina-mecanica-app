apiVersion: v1
kind: Service
metadata:
  name: datadog-agent
  namespace: oficina
  labels:
    app.kubernetes.io/name: datadog-agent
    app.kubernetes.io/part-of: oficina-mecanica

spec:
  type: ClusterIP

  selector:
    app.kubernetes.io/name: datadog-agent

  ports:
    - name: otlp-http
      protocol: TCP
      port: 4318
      targetPort: otlp-http
